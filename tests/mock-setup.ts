/**
 * Global setup for mock tests
 */

export default async (): Promise<void> => {
  console.log('🎭 Setting up mock test environment...');
  console.log('✅ Zero external dependencies - complete isolation achieved!');
};